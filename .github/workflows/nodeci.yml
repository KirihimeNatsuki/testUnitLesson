name: Node CI

on:
  push:
    branches: [ testUnitJS ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Install 🔧
        run: npm install

      - name: Test 🚨
        run: npm run test